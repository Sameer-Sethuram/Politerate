# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/
ENV/

# Local SQLite cache (regenerated by pipeline)
politerate.db
*.db-journal
*.db-wal
*.db-shm

# Claude Code local-only settings (shared settings live in .claude/settings.json)
.claude/settings.local.json

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Secrets
.env
.env.local
*.pem
*.key

# Intermediate / scratch files
resources_extracted.txt
*.tmp
outputs/

# Senior-design reference docs (UX assignments, abstract, logo PDF, etc.)
resources/

# Sarah's credibility training notebook — she maintains this on her side
politerate_credibility_model.ipynb

# Jupyter checkpoints
.ipynb_checkpoints/

# Empty placeholder model dirs (credibility model weights live elsewhere per BUG-3)
models/credibility/*.bin
models/credibility/*.safetensors

# Summarizer model folder
fine_tuned_bart_news/

# Credibility / DeBERTa weights (git-ignored; loaded at runtime)
credibility/weights/*
!credibility/weights/.gitkeep

# Dev scratch: pipeline test runners and their outputs
outputs/
